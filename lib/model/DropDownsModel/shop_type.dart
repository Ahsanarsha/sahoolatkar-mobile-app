class ShopeType {
  final String title;
  final int id;

  ShopeType({required this.title, required this.id});
}

final List<ShopeType> shopeType = [
  ShopeType(title: "Rented", id: 1),
  ShopeType(title: "Owned", id: 2),
];
